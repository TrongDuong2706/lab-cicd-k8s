apiVersion: apps/v1
kind: Deployment
metadata:
  name: uat-frontend
  namespace: my-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: uat-frontend
  template:
    metadata:
      labels:
        app: uat-frontend
    spec:
      imagePullSecrets:
        - name: ecr-regcred
      containers:
      - name: frontend
        image: 980921740481.dkr.ecr.ap-southeast-1.amazonaws.com/frontend:c0f58ee
        ports:
        - containerPort: 80
        env:
        - name: APP_API_HOST
          value: "uat-backend"
        - name: APP_API_PORT
          value: "80"