apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: backend-dev-ingress
  namespace: my-app
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: dev.trongduong.website
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: backend-dev
            port:
              number: 80