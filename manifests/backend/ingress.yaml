apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: uat-backend-ingress
  namespace: my-app
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: uat.trongduong.website
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: uat-backend
            port:
              number: 80