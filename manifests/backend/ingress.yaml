apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dev-backend-ingress
  namespace: my-app
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/use-regex: "true"   # <--- thêm dòng này
spec:
  rules:
  - host: trongduong.website
    http:
      paths:
      - path: /dev(/|$)(api)(/|$)(.*)
        pathType: ImplementationSpecific   # <--- thay Prefix bằng ImplementationSpecific
        backend:
          service:
            name: dev-backend
            port:
              number: 80
